---
import Input from './Input.astro'

const { id, class: className } = Astro.props
---
  <section id={id} class={`${className}`}>
    <h2 class='text-3xl font-bold mb-4 text-center'>Contacto</h2>
    <p class='text-lg mb-4 text-gray-300 p-4 leading-loose text-center'>¿Estás interesado en unirte a nosotros? ¡Nos encantaría saber de ti!</p>

    <form method='POST' action='/api/contacto' class='max-w-3xl mx-auto p-8 bg-tarjetas backdrop-blur-md rounded-2xl shadow-lg text-white space-y-6'>
      <div class='grid grid-cols-1 md:grid-cols-2 gap-6'>
        <Input id='iracing' label='Nombre en iRacing' name='iracing' required />
        <Input id='id' label='ID de iRacing' name='id' required />
        <Input id='discord' label='Nombre de usuario en Discord' name='discord' required />
        <Input id='email' label='Correo electrónico' name='email' type='email' />
        <Input id='irating' label='iRating y Safety aproximado' name='irating' required />

        <div class='md:col-span-2'>
          <Input id='experience' label='Experiencia en iRacing' name='experience' type='radio' values={['Sí', 'No']} required />
        </div>

        <div class='md:col-span-2'>
          <Input id='message' label='¿Quieres añadir algo más?' name='message' isTextarea rows={4} />
        </div>
      </div>

      <div class='text-center'>
        <button
          type='submit'
          class='bg-hover text-white font-semibold px-6 py-2 rounded-md hover:bg-hover/80 transition'
        >
          Enviar
        </button>
      </div>
    </form>

    <p>También puedes contactarnos a través de nuestras redes sociales:</p>
    <ul class='social-links'>
      <li><a href='https://x.com/EnduEnjoyers' target='_blank' rel='noreferrer'>Twitter</a></li>
    </ul>
  </section>
