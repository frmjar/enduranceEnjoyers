---
const { links } = Astro.props
---
  <div class='mx-auto hidden md:flex space-x-8 text-lg font-medium text-white'>
    {links.map(link => (
      <a href={link.href} class='hover:text-hover transition-colors'>
        {link.label}
      </a>
    ))}
  </div>
