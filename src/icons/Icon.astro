---
import Calendar from './Calendar.astro'
import CheckCircle from './CheckCircle.astro'
import Close from './Close.astro'
import Flag from './Flag.astro'
import Menu from './Menu.astro'
import MultiPerson from './MultiPerson.astro'
import SinglePerson from './SinglePerson.astro'
import Trophy from './Trophy.astro'
import Twitch from './Twitch.astro'
import Twitter from './Twitter.astro'

export type IconTypes = 'calendar' | 'trophy' | 'flag' | 'checkCircle' | 'close' | 'twitter' | 'menu' | 'singlePerson' | 'multiPerson' | 'twitch'

export interface Props {
  name: IconTypes
  class?: string
  size?: 'sm' | 'md' | 'lg' | 'xl'
  fill?: string
}

const {
  name,
  class: className = '',
  size = 'md',
  fill = 'currentColor',
  ...props
} = Astro.props

const iconComponents = {
  calendar: Calendar,
  trophy: Trophy,
  flag: Flag,
  checkCircle: CheckCircle,
  close: Close,
  twitter: Twitter,
  menu: Menu,
  twitch: Twitch,
  singlePerson: SinglePerson,
  multiPerson: MultiPerson
}

const IconComponent = iconComponents[name] || Calendar
---

  <IconComponent class={className} size={size} fill={fill} {...props} />
